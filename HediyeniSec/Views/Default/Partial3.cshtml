@using HediyeniSec.Models.Siniflar
@model List<IcimdenGeldi>

<div class="padding">
    <h4>En Popüler 10 Fikir</h4>
    <ul class="list list-pad">
        @{ int i = 0;}
        @foreach (var x in Model)
        {
        <li class="list-bg"><span>@{i++; } @i.</span><a href="/IcimdenGeldi/ArkadasDetay/@x.ID">@x.Baslik</a></li>
        }
    </ul>
    <a href="/IcimdenGeldi/Index/" class="link1">Tüm Fikirleri Gör >></a>
</div>

